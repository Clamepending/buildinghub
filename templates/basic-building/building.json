{
  "id": "example-building",
  "name": "Example Building",
  "version": "0.1.0",
  "category": "Community",
  "description": "Describe what this building helps agents do.",
  "status": "community",
  "trust": "manifest-only",
  "icon": "plug",
  "repo": {
    "url": "https://github.com/you/example-building",
    "manifestPath": "buildinghub/building.json",
    "readmePath": "README.md",
    "assetsPath": "assets"
  },
  "media": {
    "thumbnail": {
      "path": "assets/thumbnail.png",
      "alt": "Example Building thumbnail"
    }
  },
  "visual": {
    "shape": "plugin"
  },
  "footprint": {
    "width": 2,
    "height": 2,
    "shape": "plugin",
    "snap": "grid",
    "entrances": [
      {
        "side": "south",
        "offset": 1
      }
    ]
  },
  "access": {
    "label": "External service",
    "detail": "Describe the API, MCP, account, or local helper that must already be configured."
  },
  "tools": [
    {
      "type": "api",
      "name": "example.search",
      "detail": "Describe one agent-facing tool exposed by the service.",
      "endpoint": "example-api",
      "required": false
    }
  ],
  "endpoints": [
    {
      "type": "api",
      "name": "example-api",
      "method": "POST",
      "urlTemplate": "https://api.example.com/v1/{resource}",
      "auth": "api-key",
      "detail": "Describe the API surface the user configures outside BuildingHub.",
      "required": true
    }
  ],
  "capabilities": [
    {
      "type": "env",
      "name": "EXAMPLE_API_KEY",
      "detail": "Required where the agent or helper command runs.",
      "required": true
    }
  ],
  "onboarding": {
    "variables": [
      {
        "label": "API key",
        "value": "EXAMPLE_API_KEY in agent environment",
        "required": true,
        "secret": true
      }
    ],
    "steps": [
      {
        "title": "Connect the service",
        "detail": "Set up the external service or connector."
      },
      {
        "title": "Install the building",
        "detail": "Add the building to Agent Town after reviewing its capabilities.",
        "completeWhen": {
          "type": "installed"
        }
      }
    ]
  }
}
